# Daily Plan — 2026-09-07

Today's sprint focuses on cycle reliability rather than new surface area: three of the four open issues are about cycles failing, hanging, or losing track of themselves, and shipping a new dashboard feature (#7) on top of a shaky foundation would just mean re-testing it once the underlying reliability issues are fixed anyway.

We'll start with #46 (reject cycles with no GitHub token) since it's a small, self-contained guard that stops a whole class of confusing partial-failure cycles before they start — good warm-up and immediately reduces noise for the rest of today's work. Then #4 (Dev agent hangs on long CLI prompts) is the highest-value fix: a hung Dev node eats the full phase budget before timing out, which is the most direct tax on how many cycles we can actually complete in a day. #5 (orphan running cycles after restart) closes out the day — it's a known landmine explicitly called out in our own docs, and every push-to-main deploy is a chance to trigger it, so leaving it unfixed means today's other two fixes could themselves get orphaned mid-verification.

Main risk: #4 and #5 both touch the cycle/agent state machine (`cycle.py`, `agents/dev.py`, the in-memory tracker in `api.py`), so if they land as separate PRs there's a real chance of merge conflicts or of one fix masking the other's symptoms during verification. We should sequence the merges rather than parallelize them, and re-verify each on prod with a real cycle per AGENTS.md's 'done means' bar before starting the next.

#7 (dashboard progress visibility) and #6 (stale ANTHROPIC_API_KEY) are deferred: #7 is purely cosmetic until cycles reliably reach completion, and #6 only affects the API fallback path, which is secondary to the CLI-first billing model — not worth today's slot when core cycle correctness is at stake.
