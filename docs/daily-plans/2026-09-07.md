# Daily Plan — 2026-09-07

```json
{
  "selected": [
    {"number": 46, "title": "Reject a cycle when the target repo has no GitHub token", "reason": "Stops cycles from running against a repo they can't actually push to — a fast, cheap guardrail that prevents wasted agent time and confusing partial failures downstream. High value, low complexity, no dependencies."},
    {"number": 5, "title": "Orphan running cycles after container restart", "reason": "Known production landmine: the in-memory cycle tracker forgets running cycles on restart, so the theater silently loses track of live work. Directly affects reliability of the core product loop and has been flagged as an open gap."},
    {"number": 4, "title": "Dev agent hangs on long Claude CLI prompts", "reason": "A hang in the Dev node stalls the whole cycle within its phase budget, burning the dev_iter window with nothing shipped. Fixing this protects the core plan→implement→review→test loop that everything else depends on."}
  ],
  "daily_plan": "Today's focus is cycle reliability over new surface area: the three selected issues all protect the core plan → implement → review → test loop from silent or wasted failures, rather than adding visible features. We start with #46 since it's the cheapest guardrail — rejecting cycles up front when a repo has no usable GitHub token avoids agents doing real work that can never be pushed, and it has no dependency on the other two.\n\nNext is #5, orphaned running cycles after a container restart. This is an already-documented operational gap (the in-memory tracker forgets in-flight cycles), so the fix likely needs a small persistence or reconciliation step on startup. It's independent of #46 but should land before #4 so we don't confuse a genuinely-hung cycle with one merely orphaned by a restart when validating the fix.\n\n#4, the Dev agent hanging on long Claude CLI prompts, is the highest-effort item and the biggest risk to today's plan — CLI hangs are notoriously hard to reproduce deterministically, so we should timebox investigation and prefer a defensive timeout/kill-and-requeue fix (consistent with the existing `implement_task` requeue-to-`status:ready` pattern) over chasing a root cause in the CLI itself.\n\nDeferred: #7 (dashboard iteration progress) is a UX improvement with no correctness risk, and #6 (stale ANTHROPIC_API_KEY) only affects the fallback path while CLI billing remains primary — both are safe to push to the next sprint without blocking anything selected today."
}
```
