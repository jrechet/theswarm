# Daily Plan — 2026-09-07

Today's backlog is small but the top item matters: #70 (resume a cycle after restart) closes a landmine that's been called out since issue #5 — right now any container restart, or any commit that lands on `main` while a cycle is running, silently orphans that cycle with no way to recover it. Since the tracker is in-memory, the fix needs to persist enough cycle state (target issue, phase, workspace) to reattach after startup, and TechLead should scope whether that's a lightweight SQLite table or richer state machine before Dev starts. Land this first since it's pure reliability with no user-facing surface change.

#69 (stop watching an agent once its phase is over) is the second priority — it's a smaller, well-contained fix to the theater's live polling, but it's sequenced after #70 because both touch how the system tracks 'when is a phase actually done,' and getting resume-on-restart right first avoids having to rework the watch-stopping logic once persisted cycle state lands.

Main risk today is the usual one for this repo: any change to `agents/*.py` needs the schema guard test run early, and neither of these issues should ever write to `main` outside the normal PR flow (the `docs/daily-plans/**` paths-ignore trick only covers this file, not the agent code). If #70's persistence layer touches new tables, keep the migration additive so a mid-cycle restart during rollout doesn't itself become a data-loss risk. With only two issues in the backlog, today's real bottleneck after these ship will be backlog depth, not throughput — worth flagging for tomorrow's planning.
