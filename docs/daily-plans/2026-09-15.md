# Daily Plan — 2026-09-15

```json
{
  "selected": [
    {
      "number": 99,
      "title": "Un échec au cycle précédent ne compte pas : la tâche impossible repart en tête",
      "reason": "attempted_tasks in agents/dev.py's pick_task lives only in the dev loop's own state, so it never survives a cycle boundary. #101 (merged today) fixed the within-loop ordering — least-tried first, ready before in-progress — but a task that failed last cycle comes back next cycle with a clean slate and lands at the head of the ready tier again. An unfixable sub-task can eat every future cycle's first pick while its siblings, which could actually ship, wait behind it. Highest value: this blocks forward progress on any backlog with one bad task in it."
    },
    {
      "number": 85,
      "title": "Une sous-tâche déjà satisfaite boucle au lieu de se fermer",
      "reason": "implement_task (agents/dev.py:277-284) treats 'no file changes produced' as a bare result and returns without closing the issue or moving it off ready/in-progress — the case where Claude correctly finds the sub-task already done. The next Ralph round or cycle just re-picks the same issue and repeats. Same neighborhood as #99 (the picker/dev-loop state machine), so sequencing it right after keeps both fixes in one mental model of 'what happens to a task after an iteration that didn't produce a normal diff.'"
    }
  ],
  "daily_plan": "Both open items are the same class of bug: the dev loop's task-lifecycle bookkeeping doesn't survive past a single pass, so a task can get stuck in a state nothing exits — either retried forever (#99) or never marked done (#85). #101 landed today and fixed the *within-cycle* ordering; #99 is the cross-cycle half of the same problem, and it's the higher-value fix because one unfixable backlog item can otherwise consume the first slot of every cycle indefinitely, starving siblings that would actually ship. Start there.\n\n#85 is next and self-contained: when implement_task gets a clean 'no changes' result because the sub-task was already satisfied by earlier work, nothing closes the issue or reclassifies it, so it stays workable and gets re-picked. Fixing it right after #99 means both patches touch the same decision point (what pick_task/implement_task do with a task after a non-standard outcome) and can be reviewed together for consistency instead of drifting into two different conventions.\n\nRisk: both fixes touch attempted_tasks/label transitions that #101, #93, and #92 already modified this week — same file, same functions, high chance of a merge/logic conflict if TechLead breaks these into parallel sub-tasks. Recommend sequencing them (99 then 85) rather than parallelizing, and re-reading dev.py's current state before either breakdown since it's moved three times in three days.\n\nNo new stories queued behind these — backlog is down to these two after #101's cleanup. Once they land, worth a pass to see if anything else in the 'task never reaches a terminal state' family is still lurking before generating new feature work."
}
```
