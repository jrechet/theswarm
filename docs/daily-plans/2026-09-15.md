# Daily Plan — 2026-09-15

```json
{
  "selected": [
    {
      "number": 99,
      "title": "Un échec au cycle précédent ne compte pas : la tâche impossible repart en tête",
      "reason": "Directly affects the Dev agent's task-picking reliability; the description overlaps closely with the sibling-jump fix just merged in #101 (closes #100, part of #79), so verifying whether this is already resolved or a distinct case is high value and cheap to confirm first."
    },
    {
      "number": 85,
      "title": "Une sous-tâche déjà satisfaite boucle au lieu de se fermer",
      "reason": "A subtask that keeps re-running after it's already satisfied wastes iteration budget on every affected cycle; fixing the completion check protects throughput on every future run regardless of the #99 outcome."
    }
  ],
  "daily_plan": "Today's sprint has only two open items, both bugs in the Dev agent's iteration loop, so the priority is correctness of task selection and completion detection rather than new surface area. Start with #99: before implementing anything, TechLead should diff its description against the fix merged this morning in #101 (closed #100, attempts/tier sort key, hand-back-on-cancel) — if #99 describes the same root cause it may already be resolved and should be closed as a duplicate with a note, saving the team from re-solving a shipped bug. If #99 turns out to be a distinct trigger (e.g. a different code path for 'impossible' tasks not covered by the attempts/tier fix), scope it narrowly and add a regression test alongside the existing ones from #101.\n\nSecond priority is #85: a sub-task that's already satisfied looping instead of closing. This looks like a gap in the loop's completion check — worth checking whether it shares plumbing with the requeue-to-ready logic touched in #101, since both bugs live in the same dev-loop area and a shared root cause would mean one fix covers both.\n\nMain risk today is duplicate/overlapping work: #99 and the just-merged #101 sound like the same bug reported twice, so time could be wasted re-fixing something already live in prod. Second risk is scope creep into the broader #79 epic that #101 references — keep today's changes narrow to these two issues and avoid touching unrelated parts of the dev iteration loop. No deploy risk beyond the usual: any fix here touches `agents/dev.py`-area code, so run the full test suite and the phase-timeout paths before merging, and remember a merge to main will redeploy and can kill any cycle currently in flight."
}
```
