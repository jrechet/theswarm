# Daily Plan — 2026-09-15

```json
{
  "selected": [
    {
      "number": 85,
      "title": "Une sous-tâche déjà satisfaite boucle au lieu de se fermer",
      "reason": "Only open backlog item and it's a correctness bug in the dev iteration loop: subtasks that are already satisfied should close instead of looping, which wastes cycle budget and can starve other work. High business value (reliability of the core cycle loop) with no dependencies blocking it."
    }
  ],
  "daily_plan": "Today's sprint has a single focus: fix the sub-task loop bug in issue #85, where a sub-task whose acceptance criteria are already met keeps re-running instead of being marked closed. This sits in the same territory as the recent starvation fix (#93), so the Dev agent should check whether the same completion-detection logic in `dev_iter` is the root cause — likely a mismatch between how \"done\" is evaluated versus how the loop decides to requeue a task to `status:ready`.\n\nPriority is to land a minimal, targeted fix: correctly detect an already-satisfied sub-task and transition it to closed rather than re-queuing it, without touching the starvation-prevention logic just landed. This is a reliability fix for the cycle engine itself, so any change here should go through full CI and a real prod cycle re-verification per AGENTS.md's \"done\" definition, not just unit tests — a loop bug repro is easy to miss in isolated test cases.\n\nRisk: the backlog is thin (only one open issue), so there's no fallback work queued if #85 turns out to be more complex than expected (e.g., if it's entangled with the phase-budget or requeue logic in `implement_task`). If Dev/TechLead surface that this is bigger than a single-issue fix, we should split it into a follow-up sub-task rather than let it balloon inside one cycle. No other in-flight priorities to sequence against today."
}
```
