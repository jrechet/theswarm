# Daily Plan — 2026-09-16

Today's backlog has a single open story: #113, where `GET /api/cycles/{id}` returns inconsistent response shapes depending on whether the cycle is served from the in-memory tracker or the persisted store. This is a small but important correctness fix — any client (theater polling, CLI, external tooling) that reads this endpoint has to special-case both shapes today, which is fragile and will only get worse as more consumers show up.

Priority for the day is to get #113 fully specified, implemented, and merged: Dev should normalize the response into one canonical schema regardless of source store, and QA should add coverage for both the in-memory and persisted-store code paths so a regression here is caught automatically rather than discovered by a confused client.

Main risk is scope creep — it would be easy to turn this into a broader refactor of the cycle tracker/store split. Keep the fix narrow: same response contract from both sources, without touching the underlying storage architecture. No dependency ordering concerns since this is the only item in the sprint.

With no other backlog items ready, today's secondary goal is making sure the next cycle isn't blank: flag to the team that the backlog needs grooming so there's a queue of `status:ready` issues for tomorrow.
