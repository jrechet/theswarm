# Daily Plan — 2026-09-16

```json
{
  "selected": [
    {
      "number": 121,
      "title": "A REQUEST_CHANGES review is written on the PR and then forgotten — the task never comes back to the Dev",
      "reason": "Breaks the core review→rework loop: a REQUEST_CHANGES verdict should route the task back to the Dev agent, but it silently dead-ends instead. This blocks the cycle from ever completing on any PR that gets change requests, so it outranks the sorting bug on business value and is fixed first since it touches the same TechLead/Dev handoff path other work depends on."
    },
    {
      "number": 119,
      "title": "The merged cycle list sorts naive local and aware UTC timestamps as strings",
      "reason": "Cosmetic/correctness bug in the merged cycle list ordering (comparing datetimes as strings mixes naive-local and aware-UTC formats). Lower urgency than #121 since it doesn't block a cycle from completing, but it's a small, well-scoped fix worth picking up the same day."
    }
  ],
  "daily_plan": "Today's backlog is small — two bugs, no new features queued. The priority is #121: a REQUEST_CHANGES review currently gets written to the PR but the task state never flips back to make the Dev agent pick it up again, which means any cycle that gets change requests stalls out instead of iterating. This is a correctness bug in the TechLead→Dev handoff, the same seam that #112 and #121's neighbors have touched before, so the Dev should trace where the review verdict is persisted and confirm the issue's `status:ready` (or equivalent) transition actually fires on REQUEST_CHANGES, then add a regression test so this can't silently regress again.\n\nSecond up is #119: the merged cycle list sorts timestamps as raw strings, and since SQLite-backed cycles use naive local timestamps while the tracker's in-memory cycles use aware UTC ones, ordering is wrong whenever the two are mixed — directly downstream of the SQLite+tracker merge shipped in #117. Fix should normalize both to a single aware/naive convention before comparison rather than patching the sort key ad hoc.\n\nRisk: both fixes touch shared cycle/task state plumbing (`AgentState`, the cycles table) — changes must respect the `AgentState` schema guard and the naive/aware datetime landmines already known in this codebase. Neither issue involves a write to `main` outside the normal PR flow, so no redeploy-kills-the-cycle risk today. With only two issues in the backlog, plan to spend the rest of the day topping up the backlog from any known gaps in the review/rework loop rather than starting speculative new work."
}
```
