# Daily Plan — 2026-09-17

Today's sprint focuses on restoring trust in the Dev↔TechLead review loop before anything else: #121 is a correctness bug in the core autonomous cycle — a REQUEST_CHANGES verdict is written to the PR but the state machine never routes the task back to `status:ready` for the Dev to address it, so review feedback silently dies and PRs sit unresolved. This directly undermines the swarm's value proposition of unattended iteration, so it's the priority pull for the Dev agent today.

In parallel, #119 is a small, low-risk fix to the merged cycle list view where naive local timestamps and timezone-aware UTC timestamps are compared as strings rather than normalized before sorting, producing an incorrectly ordered list. This has no dependency on #121 and can be picked up independently, so it's included to keep throughput up without blocking on the more involved review-loop fix.

Main risk today is in #121: the fix likely touches `techlead.py`'s review-verdict handling and possibly `cycle.py`'s phase routing, both flagged in project history as areas where undeclared `AgentState` keys or missed `_announce` calls can silently break the graph — the Dev should run `tests/test_agent_state_schema.py` early per project convention. No other backlog items are ready today; both selected issues should be achievable within one cycle each.
