# Daily Plan — 2026-09-17

```json
{
  "selected": [
    {
      "number": 79,
      "title": "Feuille de route : rendre TheSwarm fiable sur des features réelles",
      "reason": "It's the only item in the open backlog — a living dashboard tracking reliability work on TheSwarm's own repo. It supersedes ad-hoc story picking: the checklist inside it already sequences today's real priorities (#143 in flight, #144 and #133 next), and every M-Swarm defect it lists was found by running the swarm on itself, which is the project's actual measurement strategy right now."
    }
  ],
  "daily_plan": "Today's backlog has exactly one open issue — #79, the standing roadmap — because the concrete work items it tracks (#143, #144, #133, #135, #121...) are sub-issues threaded through its checklist rather than separate top-level stories. A cycle on #143 (demo card shows the app: attach screenshots to the persisted report, honor `demo.pages`/`demo.setup`, fix the /docs 404 in the video walk, and stop counting held PRs as merged) is already running, launched 09:25. That stays the priority: it closes the loop on the QA visual verification work from #132/#134, and until it lands, the 'Latest demo' card on `/r/jrechet/theswarm` is misleading rather than useful.\n\nNext up once #143 lands: #144 (`/openapi.json` answers 500 — cheap, isolated, found by the demo walk itself) and #133 (the learned CLI timeout floor is forgotten on every deploy, costing ~7 minutes per self-cycle in re-timeouts before it relearns budgets). Both are small, well-scoped, and unblock faster self-cycles, which is the whole point of M-Swarm: every defect on this list so far was invisible on the easy target (`concert-tour-app`) and only surfaced by running the swarm on its own 2700+ test repo. #121 (a REQUEST_CHANGES review that's written and then never routed back to the Dev) is real but riskier to schedule blind — it touches the review→requeue path that #100/#101/#130 already hardened this week, so it should follow #143/#144/#133, not race them.\n\nRisk carried from the agent memory: quality gates (unit tests, coverage, security, e2e) have not been reliably blocking merges — PRs have merged with e2e failing and three gates `not_run`. That's a standing correctness risk for every PR this roadmap reports as 'shipped,' independent of the M-Swarm plumbing fixes. It's not yet on the roadmap as its own line item and should be, once #143 frees up a cycle slot — a merge that isn't actually gated by green CI undermines the 'verified in production' bar the whole roadmap holds itself to.\n\nMethod risk to hold today: no deploy while a measurement cycle is running (three killed this way already), and on `jrechet/theswarm` itself, only I merge approved PRs between cycles — the TechLead holds rather than merging on the self-repo because a merge to main redeploys and kills the running cycle."
}
```
