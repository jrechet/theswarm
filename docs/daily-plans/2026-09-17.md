# Daily Plan — 2026-09-17

The backlog is thin today — just one open issue, #144 (GET /openapi.json answers 500). Given the note in agent memory that 3/3 PRs merged last cycle despite failing e2e tests and not_run status on unit/coverage/security, our quality gates are not currently a reliable signal, so today's priority is not just closing #144 but making sure the Dev/QA agents actually exercise the fix (hit the endpoint, assert a 200 and valid schema) rather than relying on CI going green.

Goal for today: get #144 into status:ready, have Dev diagnose why FastAPI's schema generation is raising a 500 (likely a route with a malformed response_model, an unserializable type in a Pydantic model, or a duplicate operation id — common causes for this exact symptom), fix it, and have QA verify with a direct request to /openapi.json in addition to whatever the automated suite reports.

Risk: this is a small, isolated bug with no dependency chain, so there's low risk of scope creep, but the known gap in our quality gates means a superficially green PR isn't proof of a real fix. Since this is theswarm's own repo (SELF_REPO), remember the TechLead will approve but not merge — a person merges between cycles once the fix is confirmed against a live /openapi.json call, not just a passing test file.

With only one item, today's sprint is single-issue: ship #144, verify it manually against the running app, and leave capacity for backlog grooming since there's nothing else queued behind it.
